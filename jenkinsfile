pipeline {
  agent {
    node {
        label 'slave'
        customWorkspace '/home/jenkins'
    }
}
dockerfile {
        additionalBuildArgs  '--build-arg version=1.0.2'
        args '-v /tmp:/tmp'
    }
}
stages {
        stage('Example Build') {
            steps {
                sh 'hostname'
            }
        }
    }
