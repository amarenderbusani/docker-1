pipeline {
  agent {
    node {
        label 'slave'
        customWorkspace '/home/jenkins'
    }
}
dockerfile {
        filename 'Dockerfile.build'
        dir 'build'
        additionalBuildArgs  '--build-arg version=1.0.2'
        args '-v /tmp:/tmp'
    }
}
