pipeline {
 agent { label 'slave' }
stages {
        stage('Example Build') {
            steps {
                sh 'mvn clean'
            }
        }
    }
node {

    docker.build("test1:${env.BUILD_ID}")

    customImage.inside {
        sh 'make test'
    }
}
}
