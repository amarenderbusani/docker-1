pipeline {
 agent { label 'slave' }
stages {
        stage('Example Build') {
            steps {
                sh 'mvn clean'
            }
        }
    }
node {
checkout scm
docker.build("test1:${env.BUILD_ID}")
}
}
