pipeline {
 agent { label 'slave' }
stages {
        stage('Example Build') {
            steps {
                sh 'hostname'
            }
        }
stage('building an image') {
steps {
    def customImage = docker.build("my-image:${env.BUILD_ID}")

    customImage.inside {
        sh 'hostname'
}
}
}
}
}
