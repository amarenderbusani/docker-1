pipeline {
 agent { label 'slave' }
stages {
        stage('Example Build') {
            steps {
                sh 'mvn clean'
            }
        }
    }
node {
    checkout scm

    def customImage = docker.build("my-image:${env.BUILD_ID}")

    customImage.inside {
        sh 'hostname'
}
}
}
